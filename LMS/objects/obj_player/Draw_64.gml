// Get all keys in the struct
/*
var count = struct_names_count(items);
var keys = variable_struct_get_names(items);
array_sort(keys, true);

// Iterate over the keys
for (var i = 0; i < count; i++) {
    var key = keys[i];
    var value = items[$ key][$ "name"];
	var amount = items[$ key][$ "amount"];
	
    draw_text_transformed(16, 16 + (16 * i), string(value) + ", " + string(amount), 1, 1, 0);
}
*/